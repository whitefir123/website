# 图标资源目录

此目录用于存放网站的自定义图标资源。

## 说明

目前网站主要使用 Font Awesome 图标库（通过 CDN 引入）。

此目录用于存放：
- 自定义 SVG 图标
- 网站 favicon
- 社交媒体图标（如需自定义样式）

## 命名规范

- 使用小写字母和连字符
- 使用描述性的名称
- 示例：`custom-logo.svg`

## 推荐格式

- SVG 格式（矢量图标，可缩放）
- PNG 格式（位图图标，需要多种尺寸）
